import "./app.css";
import NavBar from "./Components/NavBar/NavBar";

type Props = {};

const App = (props: Props) => {
  return (
    <main>
      <NavBar />
    </main>
  );
};

export default App;
